// Exemplo 1: Programa simples com variáveis e função

let x = 10;
let y = 20;
let soma = x + y;

console.log(soma);

function calcular(a, b) {
    let resultado = a * b;
    return resultado;
}

let multiplicacao = calcular(5, 3);
console.log(multiplicacao);